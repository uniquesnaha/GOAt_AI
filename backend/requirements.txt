# --- RAG engine ---
# torch must stay >=2.5 — recent transformers releases hard-require it, and
# transformers itself must stay recent enough to recognize the Qwen3
# architecture (GEN_MODEL below). Neither pin is a RAG-logic choice, just
# packaging plumbing — see backend/parity/test_parity.py for what actually
# has to match the golden reference.
torch==2.5.1
sentence-transformers>=3.0.0
transformers>=4.51.0
bm25s>=0.2.0
qdrant-client>=1.10.0
numpy>=1.26,<2.0
pandas>=2.2.0
pyarrow>=16.0.0

# --- serving ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
websockets>=12.0
python-multipart>=0.0.9
